<section class="shell py-16 md:py-24">
  <div class="grid gap-6 lg:grid-cols-[0.9fr_1.1fr]">
    <div data-reveal class="rounded-[2rem] border border-white/10 bg-panel p-8">
      <p class="eyebrow">Foundation</p>
      <h2 class="section-title mt-4">BaseLayout owns the global document layer.</h2>
      <p class="mt-4 text-base leading-7 text-muted">
        Metadata, Open Graph tags, Twitter cards, canonical links, typography variables, and the
        favicon are handled once and inherited everywhere.
      </p>
    </div>
    <div class="grid gap-6 md:grid-cols-2">
      <div data-reveal class="rounded-[2rem] border border-white/10 bg-white/5 p-8">
        <div class="eyebrow">Meta</div>
        <p class="mt-4 text-lg font-semibold text-white">Canonical URL and social card generation</p>
      </div>
      <div data-reveal class="rounded-[2rem] border border-white/10 bg-white/5 p-8">
        <div class="eyebrow">Brand</div>
        <p class="mt-4 text-lg font-semibold text-white">Single-source title, description, and handle</p>
      </div>
      <div data-reveal class="rounded-[2rem] border border-white/10 bg-white/5 p-8">
        <div class="eyebrow">Type</div>
        <p class="mt-4 text-lg font-semibold text-white">Shared display, body, and mono font configuration</p>
      </div>
      <div data-reveal class="rounded-[2rem] border border-white/10 bg-white/5 p-8">
        <div class="eyebrow">UI</div>
        <p class="mt-4 text-lg font-semibold text-white">Reusable sections instead of one giant page file</p>
      </div>
    </div>
  </div>
</section>
